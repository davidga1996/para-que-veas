# Titulo

## subtitulo

texto para mostrar errores



## comando

```
git init "crear repositorio"
git status "ver estado de repositorio"
git add "dor formatos"
     . (punto) "carga todos los cambios"
     filename.ext (nombre completo) "carga un archivo en especifico"
git commit -m "explicar que cambio se hizo"
git remote add origin "direccion url del repositorio remoto"
git branch "muestra todas las ramas"
git branch -M main "crear rama main y en la actual"
git checkout nombre_de_la_rama "te cambia a la rama mencionada"
git pull "actualiza los cambios de la nuve a tu computador"
git push origin nombre_de_la_Rama "sube la rama a la nuve"
```
